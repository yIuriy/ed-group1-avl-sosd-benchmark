# Tests

Use this directory for small manual validation helpers and automated tests as the project evolves.

Keep correctness checks lightweight before any heavy benchmark or SOSD workload execution.
